{
  "name": "gaxtron-css-build",
  "private": true,
  "version": "1.0.0",
  "description": "Build-only toolchain: compiles a static Tailwind CSS bundle for the Django dashboard. Not used at runtime — Vercel's Python builder has no build-command hook, so the compiled CSS is committed to the repo (dashboard/static/css/tailwind-built.css) and rebuilt locally/in CI whenever template classes change.",
  "scripts": {
    "build:css": "tailwindcss -i dashboard/static/css/tailwind-src.css -o dashboard/static/css/tailwind-built.css --minify",
    "watch:css": "tailwindcss -i dashboard/static/css/tailwind-src.css -o dashboard/static/css/tailwind-built.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/container-queries": "^0.1.1",
    "@tailwindcss/forms": "^0.5.9",
    "tailwindcss": "^3.4.13"
  }
}
